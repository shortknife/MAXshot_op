# Identity Assets

Filesystem-managed hybrid identity records for Nexa.

Each markdown file defines one operator/customer identity with optional:
- email login
- wallet login
- linked identity posture

These files are the source of truth for lightweight hybrid identity, not a full SaaS IAM system.
