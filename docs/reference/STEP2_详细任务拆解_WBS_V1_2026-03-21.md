# Step 2 详细任务拆解 WBS V1

- Status: Frozen

## 目的

把 `/Users/alexzheng/Documents/JOB/AI_Project/MAXshot_opencode/docs/reference/STEP2_落地实施计划_V1_2026-03-21.md`
继续拆到实际开发前可直接执行的粒度。

本文件只做任务拆解，不写实现细节代码。

---

## WBS 总览

### WBS-2.0 Step 2 总目标
- 将当前规则驱动的 Step 2 升级为正式 `Conversation Context Manager`

---

## WBS-2.1 Phase A - 边界抽离

### WBS-2.1.1 识别现有 Step 2 代码边界
- 列出当前属于 Step 2 的函数
- 标记哪些实际已越界到 Step 3
- 形成文件级职责表

### WBS-2.1.2 设计新的模块目录
- 确定 `context-manager/` 目录结构
- 确定每个模块文件名
- 确定统一入口文件

### WBS-2.1.3 定义 Step 2 主入口函数签名
- 输入类型
- 输出类型
- 依赖注入方式

### WBS-2.1.4 明确旧逻辑迁移策略
- 哪些函数先包裹复用
- 哪些函数未来退化为 fallback

### 验收
- 产出 Step 2 模块边界图
- 产出文件迁移清单

---

## WBS-2.2 Phase B - Session Resolver

### WBS-2.2.1 定义 Session Resolver 输入
- Step 1 `Entry Envelope`
- 当前 session store
- pending clarification state

### WBS-2.2.2 定义 Session Resolver 输出
- `session_id`
- `thread_action`
- `confidence`
- `resolution_reason`

### WBS-2.2.3 定义 `thread_action` 枚举
- `continue`
- `fork_new`
- `reset`

### WBS-2.2.4 设计 session truth source
- 当前先复用内存态 store
- 定义未来可替换接口

### WBS-2.2.5 设计 session 生命周期
- TTL
- reset 触发条件
- pending clarification 对 session 的影响

### 验收
- Session Resolver Contract 完整
- 现有 store 能映射到新 Contract

---

## WBS-2.3 Phase C - Turn Relation Classifier

### WBS-2.3.1 固化 taxonomy
- `new_session`
- `continuation`
- `clarification_reply`
- `correction`
- `new_topic_same_window`
- `history_callback`

### WBS-2.3.2 设计 Prompt 输入
- 当前 `raw_query`
- 最近 turn 摘要
- pending clarification 摘要
- active context 摘要

### WBS-2.3.3 设计 Prompt 输出
- `type`
- `confidence`
- `reason`

### WBS-2.3.4 设计 guardrails
- 显式 reset 类命令
- 空输入
- 超低置信度 fallback

### WBS-2.3.5 设计旧规则函数退化策略
- `looksLikeFollowUp`
- `looksLikeOptionReply`
- `looksLikeStandaloneQuestion`
- `inferBusinessScope`

### 验收
- Prompt Spec 文档
- 输出 schema 文档
- 失败样例列表

---

## WBS-2.4 Phase D - Context Assembler

### WBS-2.4.1 定义 `ConversationContextState`
- 取代当前 `SessionBusinessContext`

### WBS-2.4.2 定义 `pending_clarification` 子结构
- exists
- scope
- missing_slots
- original_query

### WBS-2.4.3 定义 `registry_context` 子结构
- `active_capability_ids`
- `registry_version`

### WBS-2.4.4 定义 `memory_runtime` 子结构
- `source_policy`
- `ref_ids`
- `memory_ref_count`
- memory type 预留位

### WBS-2.4.5 定义 `effective_query` 形成规则
- 何时原样下传
- 何时通过 policy 变换
- 何时不得直接拼接

### 验收
- `Context Envelope` JSON schema 完整
- 每个字段有来源说明

---

## WBS-2.5 Phase E - Context Policy Engine

### WBS-2.5.1 continuation policy
- 哪些字段可继承
- 哪些字段允许缩小范围

### WBS-2.5.2 clarification_reply policy
- 只补缺槽
- 不重写已明确字段

### WBS-2.5.3 correction policy
- 哪些字段覆盖
- 哪些字段保留

### WBS-2.5.4 new_topic_same_window policy
- 清哪些上下文
- 保留哪些 session 元数据

### WBS-2.5.5 history_callback policy
- 何时触发 recall adapter
- 何时禁止旧 query 直接拼接

### WBS-2.5.6 new_session policy
- 何时完全新开上下文

### 验收
- Policy decision table 完整
- 每种 relation 至少一个正例和反例

---

## WBS-2.6 Phase F - Recall Adapter

### WBS-2.6.1 定义 recall 输入
- 当前 query
- 历史 session turns
- active context

### WBS-2.6.2 定义 recall 输出
- `recall_hits`
- `recall_summary`
- `recall_confidence`

### WBS-2.6.3 定义第一版数据来源
- 先用现有 session history / prior turns
- 不立即接复杂向量系统

### WBS-2.6.4 定义 recall 触发条件
- 只有 `history_callback`
- 其他 relation 不应误触发

### 验收
- recall contract 完整
- 至少两类历史回溯问题能被正确建模

---

## WBS-2.7 Phase G - 主链回接

### WBS-2.7.1 重写 Step 2 -> Step 3 交接点
- Step 3 只消费 `Context Envelope`

### WBS-2.7.2 清理旧入口逻辑
- 旧 Step 2 逻辑退为兼容层

### WBS-2.7.3 明确主链依赖图
- Step 1 -> Step 2 -> Step 3

### WBS-2.7.4 验证回归兼容性
- phase0
- phase1
- phase2
- Step 2 专项测试

### 验收
- 主链不再混 Step 2/3 职责

---

## WBS-2.8 文档与 Prompt 管理

### WBS-2.8.1 为 Step 2 建立 Prompt 文档
- `TURN_RELATION_CLASSIFIER_PROMPT_SPEC_V1`

### WBS-2.8.2 为 Step 2 建立测试矩阵
- `STEP2_测试矩阵_V1`

### WBS-2.8.3 为 Step 2 建立模块说明
- 每个子模块一页职责说明

### WBS-2.8.4 更新索引
- `docs/reference/README.md`

---

## 依赖顺序

严格依赖如下：

1. `WBS-2.1` -> `WBS-2.2`
2. `WBS-2.2` -> `WBS-2.3`
3. `WBS-2.3` -> `WBS-2.4`
4. `WBS-2.4` -> `WBS-2.5`
5. `WBS-2.5` -> `WBS-2.6`
6. `WBS-2.6` -> `WBS-2.7`
7. `WBS-2.8` 贯穿全程

---

## 完成标准

当以下条件同时满足，Step 2 才算允许进入编码：

1. Contract 已冻结
2. 实施计划已冻结
3. WBS 已冻结
4. Prompt 管理标准已冻结
5. Step 2 测试矩阵已完成
6. 各子模块输入输出结构已明确

否则不进入代码阶段。
