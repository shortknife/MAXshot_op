# Step 7 测试矩阵 V1

- Date: 2026-03-29
- Scope: `Step 7 Capability Execution`
- Status: Active

## A. Registry lookup

1. active capability id -> executes correct capability
2. missing capability id -> explicit failure
3. alias capability id -> resolves to active capability

## B. Execution semantics

1. `data_fact_query` returns stable output shape
2. `product_doc_qna` returns stable output shape
3. `content_generator` returns stable output shape
4. `context_assembler` returns stable output shape

## C. Failure semantics

1. missing capability -> `capability_not_found`
2. blocked publisher -> `pending_confirmation_required`
3. failed capability -> not silently converted to success

## D. Output integrity

1. output preserves `capability_id`
2. output preserves `capability_version`
3. output preserves `evidence`
4. output preserves `audit`
5. aggregated result preserves ordered `capability_outputs`
